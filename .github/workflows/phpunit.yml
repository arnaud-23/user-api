name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Run phpunit
      run: /bin/php /vendor/phpunit/phpunit/phpunit --configuration /phpunit.xml /tests --teamcity --cache-result-file=/.phpunit.result.cache
