The <info>%command.name%</info> command generates a new use-case for an Entity.

<info>php %command.full_name% BlogPost</info>

If the argument is missing, the command will ask for the class name interactively.
