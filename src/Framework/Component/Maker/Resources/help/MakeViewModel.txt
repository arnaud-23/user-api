The <info>%command.name%</info> command generates a new view-model.

<info>php %command.full_name% BlogPostViewModel</info>

If the argument is missing, the command will ask for the class name interactively.
